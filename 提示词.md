- - 重构前端布局并实现基础用户系统。请严格按照以下三个步骤编写代码，确保代码完整、可运行且符合项目规范。

    第一步：重构前端布局与页面切换逻辑。
    请修改 static/index.html 文件。将当前的单页结构改造为“左侧固定导航栏 + 右侧动态内容区”的布局。

    1. 左侧导航栏：宽度 240px，深色背景，高度 100%。包含四个菜单项：“首页”、“知识图谱”、“智能分析”、“登录/注册”。选中状态要有高亮效果。
    2. 右侧内容区：占据剩余空间。内部包含四个主要的 div 容器（id 分别为 home-section, graph-section, analysis-section, auth-section），默认只显示“登录/注册”界面，其他隐藏。
    3. 页面切换逻辑：编写原生 JavaScript 函数，监听左侧菜单点击事件。点击不同菜单时，隐藏所有 section，仅显示对应的 section。
    4. 迁移原有功能：将目前 index.html 中所有的 PDF 上传、分析结果展示等相关 HTML 代码，完整移动到 id 为 analysis-section 的 div 中。
    5. 新建占位页面：为 home-section（首页）和 graph-section（知识图谱）创建简单的占位 HTML 内容（例如“首页开发中”、“图谱开发中”），以便测试切换效果。auth-section 暂时也显示“请登录”。

    第二步：设计 Neo4j 用户数据模型与后端 API。
    请修改 server/main.py 和相关的工具文件，实现用户系统的后端逻辑。

    1. 数据模型：在 Neo4j 中设计 User 节点，属性包含：username (字符串，唯一索引), password (字符串，需加密存储), student_name (姓名), student_id (学号), class_name (班级)。请提供初始化数据库的 Cypher 语句或 Python 代码，用于创建唯一约束。
    2. 密码安全：引入 bcrypt 库（如果未安装请在 requirements.txt 中添加），实现密码的哈希存储和验证逻辑，严禁明文存储密码。
    3. API 接口：
       - POST /api/register：接收注册信息，检查用户名是否已存在，若否则创建新用户节点，返回成功消息。
       - POST /api/login：接收用户名和密码，验证通过后返回一个简单的 token（可以是生成的随机字符串或 JWT）和用户基本信息。
       - GET /api/user/profile：需要携带 token，返回当前用户的详细信息。
    4. 中间件：添加一个简单的 FastAPI 依赖项或中间件，用于验证请求头中的 token 是否有效，保护需要登录的接口。

    第三步：实现前端登录/注册界面与交互。
    请完善 static/index.html 中的 auth-section 部分，并编写对应的 JavaScript 逻辑。

    1. 界面设计：在 auth-section 中设计一个干净的卡片式布局，包含“登录”和“注册”两个模式的切换（可以通过 Tab 切换或两个独立表单）。
       - 注册表单字段：姓名、学号、班级、用户名、密码、确认密码。
       - 登录表单字段：用户名、密码。
    2. 交互逻辑：
       - 编写 JS 函数处理表单提交。调用后端的 /api/register 或 /api/login 接口。
       - 登录成功后，将返回的 token 和用户信息保存到浏览器的 localStorage 中。
       - 登录成功后，自动隐藏 auth-section，显示 home-section（或 analysis-section），并更新左侧导航栏的用户状态（例如显示“欢迎，xxx”）。
       - 如果访问非 auth 页面时检测到 localStorage 中没有 token，则强制跳转回 auth-section。
       - 添加“退出登录”按钮（可以放在右上角或导航栏底部），点击后清除 localStorage 并刷新页面回到登录态。

    注意事项：

    - 代码风格需保持整洁，CSS 建议使用内部 style 标签或单独的 style.css，JS 使用内部 script 或单独的 app.js。
    - 所有 API 请求需包含错误处理（try-catch），并在界面上通过 alert 或 Toast 提示用户错误信息（如“用户名已存在”、“密码错误”）。
    - 确保 Neo4j 连接配置从 .env 文件读取。